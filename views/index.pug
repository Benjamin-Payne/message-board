extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href='/new') submit new message

  ul 
    each message in messages
      li= message.added + ' ' + message.user + ': ' + message.text